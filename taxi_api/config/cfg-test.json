{
    "api": {
        "port": 5000,
        "host": "localhost",
        "version": "0.1-test",
        "database": "elasticsearch"
    },
    "datasources": {
        "elasticsearch": {
            "hosts": ["http://127.0.0.1:9200"],
            "index": "api_test"
        }
    }
}